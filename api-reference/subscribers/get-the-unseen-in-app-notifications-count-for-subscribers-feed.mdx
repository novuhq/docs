---
openapi: get /v1/subscribers/{subscriberId}/notifications/unseen
---

<Snippet file="apikey-warning.mdx" />
<RequestExample>
```java Java
import co.novu.common.base.Novu;
import co.novu.api.subscribers.responses.UnseenNotificationsCountResponse;

public class Main {
    public static void main(String[] args) {
        String apiKey = "<NOVU_API_KEY>";
        Novu novu = new Novu(apiKey);
        String subscriberId = "<SUBSCRIBER_ID>";
        UnseenNotificationsCountResponse response = novu.getSubscriberUnseenNotificationsCount(subscriberId);
    }
}
```
</RequestExample>
