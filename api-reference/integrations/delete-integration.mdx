---
openapi: delete /v1/integrations/{integrationId}
---

<RequestExample>
```javascript Node.js
import { Novu } from '@novu/node';  
export const novu = new Novu('<REPLACE_WITH_API_KEY_FROM_ADMIN_PANEL>');
await novu.integrations.delete("integrationId");
```

```php PHP
// Get integrations
use Novu\SDK\Novu;

$novu = new Novu(<NOVU_API_KEY>);
$novu->deleteIntegration($integrationId);
```

```ruby Ruby
require 'novu'

client = Novu::Client.new('<NOVU_API_KEY>')
client.delete_integration('<insert-provider-id>')
```

```python Python
from novu.api import IntegrationApi

url = "https://api.novu.co"
api_key = "<NOVU_API_KEY>"
novu = IntegrationApi(url, api_key).delete(
    integration_id="<The integration ID>",
)
```


</RequestExample>

<ResponseExample>

```json Response
{
    data: ["data"]
}
```

</ResponseExample>