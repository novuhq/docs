---
title: 'Next.js'
pageTitle: 'Get started with Inbox using Novu and Next.js'
description: 'Learn how to quickly get started with Novu.'
---

import { Accordion, Accordions } from '@/components/accordion';
import { CodeBlock } from '@/components/codeblock';
import {
  BuildWorkflowStep,
  CreateAccountStep,
  CreateSubscriberStep,
  TriggerNotificationStep,
} from '@/components/quickstart/common-steps';
import { InboxCodeBlock } from '@/components/quickstart/inbox-code';
import { Button } from '@/components/ui/button';
import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from '@/components/ui/tooltip';
import { SignedIn, SignedOut, SignInButton } from '@clerk/nextjs';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import {
  Bell,
  BellRing,
  BookOpenCheck,
  Clock,
  Code2,
  Globe2,
  Laptop,
  Layers,
  Library,
  Mail,
  MessageSquare,
  Palette,
  Phone,
  Users2,
} from 'lucide-react';
import Link from 'next/link';

## Quickstart

Before you can start integrating Novu into your application, you need to create a Novu account and set up a new workflow in the Novu Dashboard. This guide walks you through those steps.

<Steps>
  <CreateAccountStep />

  <Step title="Create a new Next.js application">
    You can use the following command to create a new Next.js application, or use your own and continue from the next step.

    ```package-install
    npx create-next-app@latest
    ```

  </Step>

<Step title="Install the Inbox package">
To install the Inbox package, run the following command:

```package-install
npm install @novu/nextjs
```

</Step>

<Step title="Create the Inbox component">
  To start using the Inbox component, you need to embed it in your application. Import the{' '}
  <Method href="/platform/inbox/react/components/inbox">{`<Inbox />`}</Method> component and pass the{' '} :tooltip[applicationIdentifier]{label="The application identifier is a unique identifier for your application. You can find it in the Novu Dashboard under the API keys page."} and{' '} :tooltip[subscriberId]{label="The subscriber ID is the unique identifier for the user in your application, typically the user's id in your database."} to the component.

  <InboxCodeBlock />
  <SignedOut>
    <div className="text-sm text-gray-500 text-center mt-[-12px]">
      [Sign in](https://dashboard-v2.novu.co/auth/sign-up) to get your own API keys
    </div>
  </SignedOut>
</Step>

  <Step title="Add the Inbox to your navbar">
    Now you can import the `NotificationInbox` component and add it to your navbar, it will automatically display the Bell icon, you can customize it later.
    
    ```tsx title="app/layout.tsx"
    import { NotificationInbox } from "@/components/inbox";

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return (
        <html lang="en">
          <body>
            <nav>
              <NotificationInbox />
            </nav>
            {children}
          </body>
        </html>
      );
    }
    ```

  </Step>

  <Step title="Create your first subscriber">
    Novu will automatically create a subscriber when a new `subscriberId` was
    detected from the `Inbox` component props.

    ```package-install
    npm run dev
    ```

  </Step>

<TriggerNotificationStep />

</Steps>

## Next steps

<Cards cols={2}>
  <Card
    title="Inbox and preferences UI components"
    icon={<Library />}
    href="/platform/inbox/overview"
  >
    Explore our full-stack UI components libraries for building in-app notifications.
  </Card>
  <Card title="Styling" icon={<Palette />} href="/inbox/react/styling">
    Customize the look and feel of your Inbox to match your application's design.
  </Card>
  <Card title="Multi-tenancy" icon={<Users2 />} href="/concepts/tenants">
    Manage multiple tenants within an organization.
  </Card>
  <Card title="Localization" icon={<Globe2 />} href="/inbox/react/localization">
    Manage localized versions of your notification templates.
  </Card>
  <Card title="Environments" icon={<Layers />} href="/concepts/environments">
    Manage multiple environments based on your application's deployment stages.
  </Card>
</Cards>
