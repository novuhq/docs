---
title: '<InboxContent />'
description: 'Learn how to use the InboxContent component with custom popovers and handle notification interactions'
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

By default, `<InboxContent />` renders the content of the `<Inbox />` popover. This component is meant to be used with a custom popover.

## Basic usage

The `<InboxContent />` component provides a flexible way to customize the inbox display while maintaining all the core notification functionality.

```tsx
import { Bell, Inbox, InboxContent } from '@novu/react';
import { Popover, PopoverContent, PopoverTrigger } from '@/components/ui/popover';

export default function CustomPopoverPage() {
  return (
    <Inbox applicationIdentifier="YOUR_APPLICATION_IDENTIFIER" subscriber="YOUR_SUBSCRIBER_ID">
      <Popover>
        <PopoverTrigger>
          <Bell />
        </PopoverTrigger>
        <PopoverContent className="h-[600px] w-[400px] p-0">
          <InboxContent />
        </PopoverContent>
      </Popover>
    </Inbox>
  );
}
```

## Advanced usage

The `<InboxContent />` component can receive all the props that `<Inbox />` does, as well as the configuration- and bell-specific props. This allows you to customize how notifications are handled and displayed.

This example demonstrates how to: 
  - Handle notification clicks.
  - Customize primary and secondary action behaviors.
  - Provide custom rendering for notifications.

```tsx
import { Bell, Inbox, InboxContent } from '@novu/react';
import { Popover, PopoverContent, PopoverTrigger } from '@/components/ui/popover';

export default function CustomPopoverPage() {
  return (
    <Inbox applicationIdentifier="YOUR_APPLICATION_IDENTIFIER" subscriber="YOUR_SUBSCRIBER_ID">
      <Popover>
        <PopoverTrigger>
          <Bell />
        </PopoverTrigger>
        <PopoverContent className="h-[600px] w-[400px] p-0">
          <InboxContent
            onNotificationClick={(notification) => {
              // your logic to handle notification click
            }}
            onPrimaryActionClick={(notification) => {
              // your logic to handle primary action click
            }}
            onSecondaryActionClick={(notification) => {
              // your logic to handle secondary action click
            }}
            renderNotification={(notification) => (
              <div>
                <h3>{notification.subject}</h3>
                <p>{notification.body}</p>
              </div>
            )}
          />
        </PopoverContent>
      </Popover>
    </Inbox>
  );
}
```
