---
title: "Conversations and identity"
pageTitle: 'Conversations and identity'
description: 'How Novu models conversation threads, lifecycle, participants, and subscriber identity.'
icon: MessagesSquare
---

## Conversations

A conversation is the stateful thread where communication happens. When a user sends a message from a connected provider, Novu creates a new conversation or loads the existing one for that thread. The conversation includes message history, metadata, participants, status, and platform context needed to process the next interaction.

A conversation can include multiple participants. The agent is one participant in the conversation, but it is not the conversation itself. This distinction matters when you inspect conversations in the dashboard or build agent behavior around conversation state. A Slack thread, email thread, or other provider-specific thread maps to a conversation in Novu automatically.

## Conversation lifecycle

Conversations follow a simple lifecycle:

- **Active**: Starts when the first message arrives. Messages, actions, and reactions update the conversation while it remains active.
- **Resolved**: The agent calls `ctx.resolve()`. The `onResolve` handler fires and an optional summary is stored.
- **Reopened**: If a user sends a new message after resolution, the conversation automatically reopens.

See [Conversation observability](/agents/operate/conversation-observability) for how to view lifecycle state in the dashboard.

## Participants and subscriber identity

Participants are the people, agents, or systems involved in a conversation. When a user messages your agent from a provider, Novu tries to resolve that platform user to a known subscriber.

- If a match exists, the handler receives subscriber information such as subscriber ID, name, or email.
- If no match exists, the user is tracked as a platform user and your handler should account for cases where subscriber data is unavailable.

Once identity is resolved later, they are upgraded to a full subscriber and future messages include their subscriber data.

Subscriber identity helps your agent personalize responses, look up account details, or decide whether a conversation should be escalated. Identity resolution is provider-aware: a Slack user and an email sender may each have different identifiers at the provider level that Novu resolves to a single subscriber.

## Conversations and workflows

ACI's conversation model and Novu's workflows are designed to work together.

A conversation can trigger a workflow. For example, a user in Slack asks your agent to send a report by email, and the agent calls `ctx.trigger()` to fire a Novu workflow.

A workflow can become a conversation. For example, a user replies to a digest email with a follow-up question, and that reply enters ACI as a new conversation with the agent.

Agent conversations and Novu workflows share the same account. You can start a workflow from a chat, or turn an email reply into a new conversation.

## Related

- [Signals](/agents/setup-your-agent/use-signals)
- [Conversation observability](/agents/operate/conversation-observability)
